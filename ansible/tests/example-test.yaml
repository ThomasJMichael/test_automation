metadata:
  name: "Test Name"
  description: "Description of the test."
  date_created: "YYYY-MM-DD"
  last_modified: "YYYY-MM-DD"

board:
  name: "xvr19"

vars:
  pconfig_path: "/path/to/pconfig/directory/{{ board.name }}.json"

tasks:
  - set_fact:
      pconfig: "{{ pconfig_path }}"

  - name: "Load pconfig variables"
    include_vars:
      file: "{{ pconfig }}"
      name: "pconfig_vars"
  ...
  # Rest of the steps

required_os:
  - rhel90
  - ubuntu2004

requires_slave_board: true

steps:
  ...
  # Test steps

dependencies:
  packages:
    - package1
    - package2

cleanup:
  ...
  # Cleanup steps

logging:
  results_directory: "../logs"
  log_file: "test_name.log"
